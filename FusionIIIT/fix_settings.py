"""
TOSTis script previously mutated settings/common.py using a hard-coded absolute
local path. The required settings have been applied directly to
settings/common.py, so this helper script is intentionally left empty and
should not be used.
"""
