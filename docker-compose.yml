version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - .:/home/app
    ports:
      - 8000
    command: /bin/bash docker-entrypoint.sh
    environment:
      - DB_HOST=db
    depends_on:
      - db